import { useEffect, useMemo, useState } from 'react';
import AdminLayout, { EmptyState, StatCard, StatusBadge } from '../../components/admin/AdminLayout';
const money=(value)=>`${Number(value||0).toLocaleString('en-US')} MKD`;
export default function AdminCustomers(){const[orders,setOrders]=useState([]);const[search,setSearch]=useState('');const[selected,setSelected]=useState(null);useEffect(()=>{fetch('/api/orders').then((r)=>r.ok?r.json():[]).then((data)=>setOrders(Array.isArray(data)?data:[]));},[]);
 const customers=useMemo(()=>{const map=new Map();orders.forEach((order)=>{const key=(order.customer_email||order.customer_phone||order.customer_name||'unknown').toLowerCase();const customer=map.get(key)||{key,name:order.customer_name||'Unknown customer',email:order.customer_email||'',phone:order.customer_phone||'',city:order.customer_city||'',orders:[],spent:0};customer.orders.push(order);if(order.status!=='cancelled')customer.spent+=Number(order.total||0);map.set(key,customer);});return [...map.values()].sort((a,b)=>b.spent-a.spent);},[orders]);
 const visible=customers.filter((c)=>`${c.name} ${c.email} ${c.phone} ${c.city}`.toLowerCase().includes(search.toLowerCase()));const repeat=customers.filter((c)=>c.orders.length>1).length;const average=customers.length?customers.reduce((s,c)=>s+c.spent,0)/customers.length:0;
 return <AdminLayout title="Customers"><div className="admin-grid-stats"><StatCard label="Customers" value={customers.length} detail="Unique customer profiles"/><StatCard label="Returning" value={repeat} detail="Placed more than one order" tone="green"/><StatCard label="Average value" value={money(average)} detail="Average spend per customer"/><StatCard label="Cities served" value={new Set(customers.map((c)=>c.city).filter(Boolean)).size} detail="Delivery locations"/></div><div className="admin-toolbar"><input className="admin-input" placeholder="Search name, email, phone, or city…" value={search} onChange={(e)=>setSearch(e.target.value)}/></div><section className="admin-panel"><div className="admin-panel-head"><div><h2>Customer directory</h2><p>{visible.length} matching customers</p></div></div>{visible.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Customer</th><th>Contact</th><th>City</th><th>Orders</th><th>Total spent</th><th></th></tr></thead><tbody>{visible.map((customer)=><tr key={customer.key}><td><strong>{customer.name}</strong></td><td>{customer.email||'—'}<br/><small>{customer.phone||''}</small></td><td>{customer.city||'—'}</td><td>{customer.orders.length}</td><td><strong>{money(customer.spent)}</strong></td><td><button className="admin-button secondary" onClick={()=>setSelected(customer)}>View</button></td></tr>)}</tbody></table></div>:<EmptyState title="No customers found" text="Customers are created automatically when they place an order."/>}</section>{selected&&<div className="admin-modal" onMouseDown={(e)=>{if(e.target===e.currentTarget)setSelected(null)}}><section className="admin-modal-card"><header className="admin-modal-head"><h2>{selected.name}</h2><button aria-label="Close" onClick={()=>setSelected(null)}>×</button></header><div className="admin-modal-body"><div className="admin-form-grid" style={{marginBottom:20}}><div className="admin-field"><label>Email</label><span>{selected.email||'—'}</span></div><div className="admin-field"><label>Phone</label><span>{selected.phone||'—'}</span></div><div className="admin-field"><label>City</label><span>{selected.city||'—'}</span></div><div className="admin-field"><label>Lifetime value</label><strong>{money(selected.spent)}</strong></div></div><h3 style={{font:'900 14px Montserrat,sans-serif',textTransform:'uppercase'}}>Order history</h3><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Order</th><th>Product</th><th>Total</th><th>Status</th></tr></thead><tbody>{selected.orders.map((order)=><tr key={order.id}><td>#{order.id}</td><td>{order.product_name||'Product'}</td><td>{money(order.total)}</td><td><StatusBadge status={order.status}/></td></tr>)}</tbody></table></div></div></section></div>}</AdminLayout>}
